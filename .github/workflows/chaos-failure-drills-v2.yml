name: Chaos and Failure Drills v2

on:
  push:
    branches:
      - so
    paths:
      - ".github/workflows/chaos-failure-drills-v2.yml"
      - "contracts/operations/chaos-failure-drill-contract.v2.json"
      - "services/import-api/internal/httpserver/interruption_drill_test.go"
      - "services/import-api/internal/parsersup/restart_drill_linux_test.go"
      - "services/import-api/internal/importflow/object_outage_drill_linux_test.go"
      - "services/import-api/internal/importflow/flow.go"
      - "services/import-api/internal/objectstore/client.go"
      - "scripts/validate-memory-os-chaos-failure-drills-v2.py"
      - "scripts/validate-memory-os-chaos-failure-drills-v2-reconcile-negative.py"
      - "scripts/validate-memory-os-operability.py"
      - "scripts/reconcile-memory-os-chaos-failure-drills-v2.py"
      - "scripts/reconcile-memory-os-chaos-authority.py"
  schedule:
    - cron: "19 19 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: chaos-failure-drills-v2-so
  cancel-in-progress: false

jobs:
  execute:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: memory_os_security
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U postgres -d memory_os_security"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      MEMORY_OS_TEST_DATABASE_URL: postgres://postgres:postgres@127.0.0.1:5432/memory_os_security
      MEMORY_OS_TEST_S3_ENDPOINT: http://127.0.0.1:9000
      MEMORY_OS_TEST_S3_ACCESS_KEY: minioadmin
      MEMORY_OS_TEST_S3_SECRET_KEY: minioadmin

    steps:
      - name: Checkout exact event source
        uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Start MinIO
        run: >-
          docker run -d --name minio -p 9000:9000
          -e MINIO_ROOT_USER=minioadmin -e MINIO_ROOT_PASSWORD=minioadmin
          minio/minio server /data

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.23.x"
          cache-dependency-path: services/import-api/go.sum

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Confirm exact clean source
        run: |
          set -euo pipefail
          git fetch origin so
          test -z "$(git status --porcelain)"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(git rev-parse origin/so)" = "$GITHUB_SHA"
          echo "SOURCE_SHA=$GITHUB_SHA" >> "$GITHUB_ENV"

      - name: Compile validators and check formatting
        run: |
          python -m py_compile \
            scripts/validate-memory-os-chaos-failure-drills-v2.py \
            scripts/validate-memory-os-chaos-failure-drills-v2-reconcile-negative.py \
            scripts/validate-memory-os-operability.py \
            scripts/reconcile-memory-os-chaos-failure-drills-v2.py \
            scripts/reconcile-memory-os-chaos-authority.py
          python scripts/validate-memory-os-chaos-failure-drills-v2-reconcile-negative.py
          test -z "$(gofmt -l \
            services/import-api/internal/httpserver/interruption_drill_test.go \
            services/import-api/internal/parsersup/restart_drill_linux_test.go \
            services/import-api/internal/importflow/object_outage_drill_linux_test.go)"
          python scripts/validate-memory-os-chaos-failure-drills-v2.py

      - name: Execute exact-source v2 drills
        id: drills
        run: |
          python - <<'PY'
          import datetime as dt
          import json
          import os
          import stat
          import subprocess
          import tempfile
          import time
          from pathlib import Path

          root = Path.cwd()
          workdir = root / "services/import-api"

          def atomic_write_text(path: Path, text: str) -> None:
              path.parent.mkdir(parents=True, exist_ok=True)
              mode = stat.S_IMODE(path.stat().st_mode) if path.exists() else 0o644
              fd, temp_name = tempfile.mkstemp(
                  dir=path.parent,
                  prefix=f".{path.name}.",
                  suffix=".tmp",
              )
              temp_path = Path(temp_name)
              try:
                  with os.fdopen(fd, "w", encoding="utf-8") as handle:
                      handle.write(text)
                      handle.flush()
                      os.fsync(handle.fileno())
                  os.chmod(temp_path, mode)
                  os.replace(temp_path, path)
              finally:
                  if temp_path.exists():
                      temp_path.unlink()

          source_sha = subprocess.check_output(
              ["git", "rev-parse", "HEAD"], cwd=root, text=True
          ).strip()
          definitions = [
              (
                  "api-graceful-interruption-drain",
                  ["go", "test", "./internal/httpserver", "-run", "^TestHTTPServerGracefulInterruptionDrainsInFlightRequest$", "-count=1"],
              ),
              (
                  "parser-restart-after-protocol-failure",
                  ["go", "test", "./internal/parsersup", "-run", "^TestSupervisorRestartsSameSpoolAfterFailedAttempt$", "-count=1"],
              ),
              (
                  "object-store-outage-and-recovery",
                  ["go", "test", "./internal/importflow", "-run", "^TestFlowRecoversAfterObjectStoreOutage$", "-count=1"],
              ),
          ]
          scenarios = []
          diagnostics = []
          success = True
          for scenario_id, command in definitions:
              started = time.monotonic()
              completed = subprocess.run(
                  command,
                  cwd=workdir,
                  env=os.environ.copy(),
                  text=True,
                  stdout=subprocess.PIPE,
                  stderr=subprocess.STDOUT,
                  check=False,
              )
              duration = round(time.monotonic() - started, 6)
              passed = completed.returncode == 0
              success = success and passed
              scenario = {
                  "scenarioId": scenario_id,
                  "result": "PASS" if passed else "FAIL",
                  "integrityResult": "PASS" if passed else "FAIL",
                  "exitCode": completed.returncode,
                  "durationSeconds": duration,
              }
              if scenario_id == "object-store-outage-and-recovery" and passed:
                  scenario["assertions"] = {
                      "durablePreviewRowsDuringOutage": 0,
                      "spoolEntriesDuringOutage": 0,
                      "durablePreviewRowsAfterRecovery": 1,
                      "acceptedRecordsAfterRecovery": 2,
                      "rejectedRecordsAfterRecovery": 1,
                  }
              scenarios.append(scenario)
              diagnostics.append({
                  "scenarioId": scenario_id,
                  "exitCode": completed.returncode,
                  "output": completed.stdout[-4000:],
              })

          for scenario_id in (
              "database-loss-or-failover",
              "mixed-version-failure-and-rollback",
          ):
              scenarios.append({
                  "scenarioId": scenario_id,
                  "result": "NOT_RUN",
                  "integrityResult": "NOT_RUN",
                  "exitCode": None,
                  "durationSeconds": 0,
              })

          generated_at = dt.datetime.now(dt.timezone.utc).isoformat().replace("+00:00", "Z")
          diagnostic = {
              "schemaVersion": "memory-os-validator-diagnostic.v1",
              "workflow": "chaos-and-failure-drills-v2",
              "sourceCommitSha": source_sha,
              "result": "PASS" if success else "FAIL",
              "generatedAt": generated_at,
              "scenarios": diagnostics,
              "containsSecrets": False,
              "productionEvidence": False,
          }
          diagnostic_path = root / "docs/fixtures/memory-os-operability/chaos-failure-drill-v2-diagnostic.last.json"
          atomic_write_text(
              diagnostic_path,
              json.dumps(diagnostic, indent=2, ensure_ascii=False) + "\n",
          )

          if success:
              result = {
                  "schemaVersion": "memory-os-chaos-failure-drill-results.v2",
                  "commitSha": source_sha,
                  "generatedAt": generated_at,
                  "environment": {
                      "mode": "GITHUB_ACTIONS_UBUNTU_LINUX_POSTGRES16_MINIO",
                      "productionEvidence": False,
                      "containsSecrets": False,
                      "syntheticDataOnly": True,
                  },
                  "scenarios": scenarios,
                  "overallResult": "PARTIAL_PASS",
                  "limitations": [
                      "not production chaos",
                      "database failover not executed",
                      "mixed-version failure not executed",
                      "object outage uses an unreachable client endpoint rather than killing the MinIO process",
                      "parser restart matrix incomplete",
                      "multi-instance API interruption not executed",
                      "ephemeral PostgreSQL 16 and MinIO only",
                  ],
              }
              result_path = root / "docs/fixtures/memory-os-operability/chaos-failure-drill-results.v2.sample.json"
              atomic_write_text(
                  result_path,
                  json.dumps(result, indent=2, ensure_ascii=False) + "\n",
              )

          with Path(os.environ["GITHUB_OUTPUT"]).open("a", encoding="utf-8") as output:
              output.write(f"source_sha={source_sha}\n")
              output.write(f"success={'true' if success else 'false'}\n")
          PY

      - name: Validate generated exact-source v2 evidence
        if: steps.drills.outputs.success == 'true'
        env:
          EXPECTED_COMMIT_SHA: ${{ steps.drills.outputs.source_sha }}
        run: python scripts/validate-memory-os-chaos-failure-drills-v2.py

      - name: Commit v2 result or bounded failure diagnostic
        run: |
          set -euo pipefail
          test "$SOURCE_SHA" = "${{ steps.drills.outputs.source_sha }}"
          git fetch origin so
          if [[ "$(git rev-parse HEAD)" != "$SOURCE_SHA" ]] || [[ "$(git rev-parse origin/so)" != "$SOURCE_SHA" ]]; then
            echo "so advanced while the v2 failure drills ran; refusing stale evidence or diagnostic."
            exit 75
          fi
          git add docs/fixtures/memory-os-operability/chaos-failure-drill-v2-diagnostic.last.json
          if [ "${{ steps.drills.outputs.success }}" = "true" ]; then
            git add docs/fixtures/memory-os-operability/chaos-failure-drill-results.v2.sample.json
          fi
          if git diff --cached --quiet; then
            echo "v2 failure-drill evidence is already current."
            echo "EVIDENCE_COMMIT_SHA=$SOURCE_SHA" >> "$GITHUB_ENV"
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            if [ "${{ steps.drills.outputs.success }}" = "true" ]; then
              git commit -m "test(chaos): record exact-source object outage recovery"
            else
              git commit -m "test(chaos): record v2 failure-drill diagnostic"
            fi
            EVIDENCE_COMMIT_SHA="$(git rev-parse HEAD)"
            git fetch origin so
            if [[ "$(git rev-parse origin/so)" != "$SOURCE_SHA" ]]; then
              echo "so advanced before v2 failure-drill evidence push; refusing stale evidence."
              exit 75
            fi
            git push origin HEAD:so
            echo "EVIDENCE_COMMIT_SHA=$EVIDENCE_COMMIT_SHA" >> "$GITHUB_ENV"
          fi

      - name: Reconcile v2 operability authority
        if: steps.drills.outputs.success == 'true'
        run: |
          set -euo pipefail
          git fetch origin so
          test "$(git rev-parse HEAD)" = "$EVIDENCE_COMMIT_SHA"
          if [[ "$(git rev-parse origin/so)" != "$EVIDENCE_COMMIT_SHA" ]]; then
            echo "so advanced before v2 failure-drill authority reconciliation; refusing stale authority."
            exit 75
          fi
          python scripts/reconcile-memory-os-chaos-failure-drills-v2.py
          EXPECTED_COMMIT_SHA="$SOURCE_SHA" python scripts/validate-memory-os-chaos-failure-drills-v2.py
          python scripts/validate-memory-os-operability.py
          git add contracts/operations/production-operability-status.json
          if git diff --cached --quiet; then
            echo "Chaos/failure-drill v2 authority is already current."
          else
            git config user.name "github-actions[bot]"
            git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
            git commit -m "docs(chaos): reconcile object outage recovery evidence"
            git fetch origin so
            if [[ "$(git rev-parse origin/so)" != "$EVIDENCE_COMMIT_SHA" ]]; then
              echo "so advanced before v2 failure-drill authority push; refusing stale authority."
              exit 75
            fi
            git push origin HEAD:so
          fi

      - name: Fail when a v2 drill failed
        if: steps.drills.outputs.success != 'true'
        run: exit 1
